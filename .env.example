# ─────────────────────────────────────────────────────────────
# THE MAHIR TECH — Environment variables
# Copy to .env.local for local dev. In Coolify, add these in the
# service's Environment Variables panel.
# ─────────────────────────────────────────────────────────────

# Public site URL (used for canonical URLs, sitemap, OG). No trailing slash.
NEXT_PUBLIC_SITE_URL=https://themahirtech.com

# ── Lead form email (Resend) ──────────────────────────────────
# Server-side, so the API key stays secret. Get it at https://resend.com.
# Without it, leads are logged to the server console (form still works).
RESEND_API_KEY=
# Recipient inbox (the Resend account owner email works before domain verify):
LEAD_TO_EMAIL=mahirtechofficial@gmail.com
# OPTIONAL — only after verifying themahirtech.com at resend.com/domains:
#   branded sender + a CC copy to a second inbox
# LEAD_FROM_EMAIL=The Mahir Tech <leads@themahirtech.com>
# LEAD_CC_EMAIL=m.sohail7797@gmail.com

# ── Sanity CMS (optional) ─────────────────────────────────────
# Leave blank to run on built-in seed content. Set these to enable
# the CMS and the /studio editor. Create a project at https://sanity.io.
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-10-01
